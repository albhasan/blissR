#test_Loader.R
library(testthat)
library(blissR)

#files <- list.files(path = "/mnt/lun0/csv4scidb", full.names = TRUE)
#scidbhost <- "gis-obama.uni-muenster.de"
#scidbport <-8080L

#ml <- new("Loader", files = files, scidbhost = scidbhost, scidbport = scidbport)
#scidbInstance <- new("ScidbInstance", host = scidbhost, port = scidbport)
#loadData(ml)

